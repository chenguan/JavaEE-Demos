<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yew1eb.dao.AcctLoginLogRiskDao">
    <sql id="allfields">
        id, origin_id, acct_id, acct_login, device_id, device_uuid, token, ip, city, source, device_snapshot, ctime, utime, risk_type
    </sql>

    <insert id="batchInsert">
        INSERT INTO acct_login_log_risk
        (<include refid="allfields"/>)
        VALUES
        <foreach collection="list" item="AcctLoginLogRisk" index="index" separator=",">
            (
            null,
            #{AcctLoginLogRisk.originId},
            #{AcctLoginLogRisk.acctId},
            #{AcctLoginLogRisk.acctLogin},
            #{AcctLoginLogRisk.deviceId},
            #{AcctLoginLogRisk.deviceUuid},
            #{AcctLoginLogRisk.token},
            #{AcctLoginLogRisk.ip},
            #{AcctLoginLogRisk.city},
            #{AcctLoginLogRisk.source},
            #{AcctLoginLogRisk.deviceSnapshot},
            #{AcctLoginLogRisk.ctime},
            UNIX_TIMESTAMP(NOW()),
            #{AcctLoginLogRisk.riskType}
            )
        </foreach>
    </insert>
</mapper>

